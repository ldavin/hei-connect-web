- model_class = Teacher
.content-box
  .content-box-header
    =t '.title', :default => model_class.model_name.human
  .padded
    %p
      %strong= model_class.human_attribute_name(:name) + ':'
      %br
      = @teacher.name

    .form-actions
      = link_to t('.back', :default => t("helpers.links.back")), admin_teachers_path, :class => 'btn'
      = link_to t('.edit', :default => t("helpers.links.edit")), edit_admin_teacher_path(@teacher), :class => 'btn'
      = link_to t('.destroy', :default => t("helpers.links.destroy")), admin_teacher_path(@teacher), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
