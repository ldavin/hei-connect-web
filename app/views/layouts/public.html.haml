!!! 5
%html(lang="fr")
  %head
    %meta(charset="utf-8")
    %meta(http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1")
    %meta(name="viewport" content="width=device-width, maximum-scale=1, initial-scale=1, user-scalable=0")
    = csrf_meta_tags
    %link(href="#{image_path('favicon.ico')}" rel="icon")
    %link(href="#{image_path('apple-touch-logo.png')}" rel="apple-touch-icon")
    %link(href="#{image_path('play-store-logo.png')}" rel="android-touch-icon")

    %title= content_for?(:title) ? yield(:title) : 'HEI-Connect'

    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
      = javascript_include_tag 'https://cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js'
      = javascript_include_tag 'excanvas'
    = stylesheet_link_tag 'app', :media => 'all'


  %body#body{'data-controller' => controller_name, 'data-action' => action_name}

    = render partial: 'layouts/mobile_banner'

    .container
      .row
        .col-md-12

          %h1.text-center HEI-Connect
          %h2.text-center Un ecampus enfin simple et rapide
          = bootstrap_flash
          = yield

    = render partial: 'layouts/footer'

    / Javascripts
    = javascript_include_tag 'public'
    = render partial: 'layouts/uservoice_anonymous'
    = render partial: 'layouts/analytics'