= render partial: 'calendar_widget', locals: { courses: @courses }

= render partial: 'average_widget', locals: { session: @session }

= render partial: 'absences_widget', locals: { session: @session }

= render partial: 'status_widget', locals: { updates: @updates }
