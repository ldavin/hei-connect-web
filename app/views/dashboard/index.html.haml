.content-box
  .content-box-header
    %i.icon-info-sign
    En reconstruction
  .padded
    :markdown
      Prochainement ici, tout plein d'autres widgets super géniaux (moyenne, heures d'absence)...

= render partial: 'shared/calendar_widget', locals: { courses: @courses }

= render partial: 'shared/average_widget', locals: { session: @session }

= render partial: 'shared/status_widget', locals: { session: @session }
